Discussed configuring 3 machines (2 Linux notebooks + 1 Windows) to share memory and enable remote execution. Clarified that episodic memory already syncs conversation history automatically across all machines via Claude account. Discovered SSH MCP servers (ssh-mcp-server, ssh-mcp) that enable remote command execution between machines as an alternative to native teleport feature. User learned about automatic conversation archiving and semantic search retrieval.