Designed a Linux Beeper updater system with three components: Bauh (GUI notifications), AM (CLI package manager), and a custom `update-beeper` bash script. The script intelligently checks AUR for updates and falls back to direct AppImage downloads when the AUR package is behind upstream releases, avoiding fragile pacman database hacks. Installation involves paru, wget, and script creation.