---
name: User prefers action over discussion
description: User gives direct commands and expects immediate execution — don't ask for confirmation on obvious tasks, just do them
type: feedback
---

Act on requests immediately without asking for confirmation on straightforward tasks.

**Why:** Throughout this session the user gave direct commands ("install it", "remove it", "save insights", "act on the insights") and expected execution, not discussion. When insights suggested something useful (like installing the user's own cortex-claude instead of michaelv2's), the user said "please act" — meaning do it, don't just mention it.

**How to apply:** When the task is clear and reversible (installing packages, editing configs, pushing to repos the user owns), execute immediately. Only pause for confirmation on truly destructive or ambiguous actions. When presenting insights that suggest an action, offer to execute rather than just inform.
