# Extra env variables
# env = MY_GLOBAL_ENV,setting

# Wayland environment variables for better app compatibility
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

# Qt Wayland support
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_QPA_PLATFORMTHEME,qt5ct

# GTK Wayland support
env = GDK_BACKEND,wayland,x11
env = SDL_VIDEODRIVER,wayland
env = CLUTTER_BACKEND,wayland
# Input method (fcitx5) - required for cedilla compose across all toolkits
env = XMODIFIERS,@im=fcitx
env = QT_IM_MODULE,fcitx
env = GTK_IM_MODULE,fcitx
env = INPUT_METHOD,fcitx
env = SDL_IM_MODULE,fcitx


# Browser and app Wayland support
env = MOZ_ENABLE_WAYLAND,1
env = ELECTRON_OZONE_PLATFORM_HINT,wayland

# Cursor configuration
# wayland-cedilla-fix env vars already set above (lines 20-24), duplicates removed
