# Application bindings
$terminal = uwsm app -- $TERMINAL
$browser = omarchy-launch-browser

bindd = SUPER, RETURN, Terminal, exec, $terminal --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, F, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER SHIFT, B, Browser, exec, $browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, $browser --private
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, T, Activity, exec, omarchy-launch-tui btop
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus signal "uwsm app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus obsidian "uwsm app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, slash, Passwords, exec, uwsm app -- 1password

# If your web app url contains #, type it as ## to prevent hyperland treat it as comments
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT ALT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-or-focus-webapp YouTube "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Keyboard backlight controls (F5 = decrease, F6 = increase) with OSD
bindeld = , XF86KbdBrightnessDown, Keyboard backlight down, exec, ~/bin/kbd-backlight down
bindeld = , XF86KbdBrightnessUp, Keyboard backlight up, exec, ~/bin/kbd-backlight up

# Keyboard layout switcher
bindd = SUPER, SPACE, Switch keyboard layout, exec, hyprctl switchxkblayout apple-inc.-apple-internal-keyboard-/-trackpad next

# Display scale switcher
bindd = SUPER SHIFT, S, Display scale, exec, omarchy-scale-switcher

# Workspace 10 bindings
bindd = SUPER, code:19, Switch to workspace 10, workspace, 10
bindd = SUPER SHIFT, code:19, Move window to workspace 10, movetoworkspace, 10

# Advanced workspace navigation
bindd = SUPER CTRL, W, Workspace switcher, exec, ~/.local/bin/hypr-workspace-switcher
bindd = SUPER CTRL, N, Next non-empty workspace, exec, ~/.local/bin/hypr-next-nonempty next
bindd = SUPER CTRL SHIFT, N, Previous non-empty workspace, exec, ~/.local/bin/hypr-next-nonempty prev

# Auto-organize workspaces
bindd = SUPER, A, Auto-organize workspaces, exec, ~/.local/bin/hypr-workspace-organizer

# Clipboard history
bindd = SUPER, V, Clipboard history, exec, cliphist list | walker --dmenu | cliphist decode | wl-copy

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER SHIFT, SPACE
# bindd = SUPER SHIFT, SPACE, Omarchy menu, exec, omarchy-menu

# === Warp Agent: Screenshot bindings (grim+slurp+swappy) BEGIN ===
# Print: select region → clipboard
bindd = , Print, Screenshot (region → clipboard), exec, grim -g "$(slurp)" - | wl-copy
# Shift+Print: select region → file under ~/Pictures/Screenshots
bindd = SHIFT, Print, Screenshot (region → file), exec, grim -g "$(slurp)" "$HOME/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png"
# Ctrl+Print: select region → swappy annotator
bindd = CTRL, Print, Screenshot (region → annotate), exec, grim -g "$(slurp)" - | swappy -f -
# === Warp Agent: Screenshot bindings (grim+slurp+swappy) END ===