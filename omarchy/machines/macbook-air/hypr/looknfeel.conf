# MacBook Air look and feel
# Optimized for Intel HD Graphics 6000 (low-power iGPU)

general {
  # Default gaps (uncomment to customize)
  # gaps_in = 0
  # gaps_out = 0
  # layout = master
}

decoration {
  # Disable shadows for better performance on Intel HD 6000
  shadow {
    enabled = false
  }

  # Minimal blur for performance
  blur {
    enabled = false        # Disabled for maximum performance
    size = 3               # Low blur radius
    passes = 1             # Single pass to reduce GPU load
    new_optimizations = true
  }
}

# Lightweight animations for older hardware
animations {
  enabled = true

  bezier = smoothOut, 0.36, 0, 0.66, -0.56
  bezier = smoothIn, 0.25, 1, 0.5, 1
  bezier = linear, 0, 0, 1, 1

  animation = windows, 1, 4, smoothIn, slide
  animation = windowsOut, 1, 4, smoothOut, slide
  animation = border, 1, 5, linear
  animation = fade, 1, 5, smoothIn
  animation = workspaces, 1, 4, smoothIn, slide
}

# Performance settings for Intel iGPU
misc {
  vfr = true    # Variable frame rate - save power when idle
  vrr = 0       # VRR off for Intel iGPU compatibility
}

cursor {
  no_hardware_cursors = false
  zoom_factor = 1.0
  zoom_rigid = false
}
